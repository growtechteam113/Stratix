{
  "name": "@stratix/database",
  "version": "0.0.1",
  "private": true,
  "description": "Prisma database schema for STRATIX AI",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push --skip-generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "seed": "tsx seeds/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0"
  },
  "devDependencies": {
    "prisma": "^5.7.0",
    "tsx": "^4.7.0"
  }
}
