{
  "name": "@stratix/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared utilities and helpers for STRATIX AI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stratix/types": "*"
  },
  "devDependencies": {
    "@stratix/config": "*",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
